# 小乐 AI 下一步开发计划

**更新日期：** 2025-11-12  
**版本：** v0.8.0 规划

---

## 🎯 核心功能路线图

### Phase 1: 语音交互功能 ⭐ 优先级最高
**目标：** 实现完整的语音对话体验
**预计时间：** 2-3 天

#### 功能清单
- [ ] 前端语音输入（Web Speech API）
  - [ ] 麦克风按钮 UI
  - [ ] 实时识别显示
  - [ ] 按住/点击说话模式
  - [ ] 语音波形动画
  
- [ ] 前端语音输出（Web Speech API）
  - [ ] 自动播放开关
  - [ ] 语速调节
  - [ ] 音量控制
  - [ ] 播放状态显示

- [ ] 备选方案（如 Web API 不稳定）
  - [ ] 百度语音识别接入
  - [ ] 阿里云 TTS 接入

#### 技术要点
```javascript
// Web Speech API 示例
const recognition = new webkitSpeechRecognition();
recognition.lang = 'zh-CN';
recognition.continuous = true;
recognition.onresult = (event) => {
  const transcript = event.results[0][0].transcript;
  // 发送到后端
};

const utterance = new SpeechSynthesisUtterance(text);
utterance.lang = 'zh-CN';
speechSynthesis.speak(utterance);
```

#### 验收标准
- 用户可以通过语音输入问题
- 小乐的回复自动语音播放
- 支持中断和重新开始
- 识别准确率 > 85%

---

### Phase 2: 多轮任务规划 ⭐ 优先级高
**目标：** AI 理解复杂任务并自动拆解执行
**预计时间：** 5-7 天

#### 功能清单
- [ ] 任务识别和解析
  - [ ] 识别复杂任务意图
  - [ ] AI 自动拆解步骤
  - [ ] 生成任务依赖关系

- [ ] 数据库设计
  ```sql
  CREATE TABLE tasks (
      id SERIAL PRIMARY KEY,
      user_id VARCHAR(50),
      session_id VARCHAR(50),
      title VARCHAR(200),
      description TEXT,
      status VARCHAR(20), -- pending/in_progress/waiting/completed/failed
      parent_id INT,
      order_num INT,
      result TEXT,
      created_at TIMESTAMP,
      updated_at TIMESTAMP,
      completed_at TIMESTAMP
  );
  
  CREATE TABLE task_steps (
      id SERIAL PRIMARY KEY,
      task_id INT REFERENCES tasks(id),
      step_num INT,
      description TEXT,
      status VARCHAR(20),
      result TEXT,
      created_at TIMESTAMP
  );
  ```

- [ ] 任务执行引擎
  - [ ] 任务队列管理
  - [ ] 顺序执行逻辑
  - [ ] 用户确认机制（需要时）
  - [ ] 失败重试策略
  - [ ] 超时处理

- [ ] 前端界面
  - [ ] 任务列表面板
  - [ ] 进度条和状态显示
  - [ ] 任务详情弹窗
  - [ ] 手动控制（暂停/继续/取消）

- [ ] 工具集成
  - [ ] 自动调用相关工具
  - [ ] 工具执行结果处理
  - [ ] 错误处理和反馈

#### 示例场景
```
用户: "帮我准备周末的野餐"

AI 解析:
┌─────────────────────────────────┐
│ 任务：准备周末野餐               │
├─────────────────────────────────┤
│ 1. [待确认] 确认时间和地点        │
│ 2. [待执行] 查询天气预报          │
│ 3. [待执行] 列出需要准备的物品    │
│ 4. [待执行] 设置购物提醒          │
│ 5. [待执行] 设置出发提醒          │
└─────────────────────────────────┘

执行流程:
1. AI: "好的，请问您计划什么时候去？地点是哪里？"
   用户: "周六上午，公园"
2. AI 调用天气工具查询周六天气
3. AI 生成物品清单并展示
4. AI 创建提醒...
```

#### 验收标准
- 能识别并拆解复杂任务
- 任务步骤合理且可执行
- 执行过程可视化
- 支持暂停和恢复
- 失败能自动重试或人工介入

---

### Phase 3: 长文本总结功能
**目标：** 智能处理和总结长文档
**预计时间：** 3-4 天

#### 功能清单
- [ ] 文件上传
  - [ ] 支持 PDF/DOCX/TXT/MD
  - [ ] 拖拽上传
  - [ ] 文件大小限制（10MB）
  - [ ] 格式验证

- [ ] 文本提取
  - [ ] PDF 解析（PyPDF2）
  - [ ] Word 解析（python-docx）
  - [ ] 文本预处理

- [ ] 智能总结
  - [ ] 分块处理（每块 4000 字）
  - [ ] 多级总结策略
  - [ ] 关键信息提取
  - [ ] 要点列表生成

- [ ] 结果展示
  - [ ] 总结文本显示
  - [ ] 原文对比视图
  - [ ] 保存到记忆
  - [ ] 导出功能

#### 技术实现
```python
# 分块总结算法
def summarize_long_text(text, chunk_size=4000):
    chunks = split_text(text, chunk_size)
    
    # 第一级：分段摘要
    summaries = []
    for chunk in chunks:
        summary = ai_summarize(chunk)
        summaries.append(summary)
    
    # 第二级：合并摘要
    if len(summaries) > 1:
        combined = "\n".join(summaries)
        final_summary = ai_summarize(combined)
    else:
        final_summary = summaries[0]
    
    return final_summary
```

#### 验收标准
- 支持常见文档格式
- 总结准确且简洁
- 保留关键信息
- 处理速度合理（< 30秒）

---

## 🗓️ 开发时间表

### Week 1: 语音交互
- **Day 1-2:** 前端语音输入/输出实现
- **Day 3:** 功能测试和优化
- **Day 4:** 备选方案准备（如需要）

### Week 2: 多轮任务规划（核心）
- **Day 1:** 数据库设计和迁移
- **Day 2:** 任务解析逻辑
- **Day 3-4:** 执行引擎实现
- **Day 5:** 前端界面
- **Day 6-7:** 集成测试和完善

### Week 3: 长文本总结
- **Day 1:** 文件上传和解析
- **Day 2-3:** 总结算法实现
- **Day 4:** 前端展示和测试

---

## 📦 技术栈

### 新增依赖
```txt
# 语音相关（前端）
- Web Speech API（浏览器原生）

# 文档处理
PyPDF2==3.0.1          # PDF 解析
python-docx==1.1.0     # Word 解析
pdfplumber==0.10.3     # PDF 高级解析（备选）

# 任务管理
- 使用现有 PostgreSQL
```

---

## 🎓 学习资源

### Web Speech API
- MDN 文档: https://developer.mozilla.org/en-US/docs/Web/API/Web_Speech_API
- 中文教程: https://www.ruanyifeng.com/blog/2015/12/web-speech-api.html

### 任务规划参考
- LangChain AgentExecutor
- AutoGPT 架构
- BabyAGI 实现

---

## ⚠️ 风险评估

### 语音交互
- **风险：** 浏览器兼容性问题
- **应对：** 准备百度/阿里云 API 备选方案

### 多轮任务规划
- **风险：** 任务拆解不合理
- **应对：** 人工确认机制 + 示例训练

### 长文本总结
- **风险：** Token 限制
- **应对：** 分块处理 + 多级总结

---

## 📝 备注

1. 所有新功能都需要完整的测试用例
2. 保持向后兼容，不破坏现有功能
3. 及时更新文档和 CHANGELOG
4. 每个 Phase 完成后打 tag 发布

**开发原则：**
- 优先保证稳定性
- 用户体验第一
- 代码简洁可维护
