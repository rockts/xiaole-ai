# 文档导航栏更新指南

## 方式1: 手动更新(推荐小改动)

直接编辑 `_sidebar.md` 文件,添加新文档链接:

```markdown
* **🔧 后端开发**
  * [新文档标题](backend/new-doc.md)
```

## 方式2: 自动生成(批量添加)

运行脚本自动扫描所有 markdown 文件并生成导航栏:

```bash
# 生成 _sidebar.md
python3 scripts/generate-sidebar.py

# 预览效果
python3 -m http.server 8080

# 提交更新
git add _sidebar.md
git commit -m "docs: 更新导航栏"
git push
```

### 脚本配置

在 `scripts/generate-sidebar.py` 中可以配置:

- `CATEGORIES`: 文档分类及图标
- `IGNORE`: 忽略的目录
- `IGNORE_FILES`: 忽略的文件
- `format_title()`: 自定义标题格式化规则

### 自定义标题

如果自动生成的标题不满意,可以:

1. 运行脚本生成基础结构
2. 手动调整标题文本
3. 提交修改后的 `_sidebar.md`

## 方式3: Git Hook 自动化(高级)

创建 pre-commit hook 自动检测新文件并更新导航栏:

```bash
# 创建 hook
cat > .git/hooks/pre-commit << 'EOF'
#!/bin/bash
# 检测是否有新的 .md 文件
if git diff --cached --name-only | grep -q '\.md$'; then
    python3 scripts/generate-sidebar.py
    git add _sidebar.md
fi
EOF

chmod +x .git/hooks/pre-commit
```

## 推荐工作流

日常使用建议:

1. **少量文档更新**: 手动编辑 `_sidebar.md`(快速精确)
2. **批量迁移文档**: 运行脚本自动生成,再手动微调
3. **新分类目录**: 先在脚本中配置 `CATEGORIES`,再运行生成

## 注意事项

- 自动生成会**覆盖**现有 `_sidebar.md`,请先备份手动调整的内容
- 脚本按文件名字母排序,如需自定义顺序请手动调整
- 中文文件名会保留原样,英文文件名会自动格式化
